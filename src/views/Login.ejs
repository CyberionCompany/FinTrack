<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="LoginAssets/css/main.css" /> 
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <title>Login | Finance Tracker</title>
</head>
<body>
    <main class="login-container">
        <section class="login-promo">
            <div class="promo-content">
                <h1>Finance Tracker</h1>
                <p>O controle total das suas finanças. De forma simples e visual.</p>
                <svg class="promo-icon" xmlns="http://www.w3.org/2000/svg" width="150" height="150" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><path d="M21.21 15.89A10 10 0 1 1 8 2.83"/><path d="M22 12A10 10 0 0 0 12 2v10z"/></svg>
            </div>
        </section>

        <section class="login-form-area">
            <div class="form-wrapper">
                <h2>Acesse sua conta</h2>
                <p>Bem-vindo de volta!</p>
                
                <div class="messages-area">
                    <% if (typeof erros !== 'undefined' && erros.length > 0) { %>
                        <% erros.forEach(err => { %>
                           <div class="alert alert-danger" role="alert">
                               <%= err %>
                           </div>
                        <% }) %>
                    <% } %>
                    
                    <% if (typeof success !== 'undefined' && success.length > 0) { %>
                        <% success.forEach(scs => { %>
                            <div class="alert alert-success" role="alert">
                                <%= scs %>
                            </div>
                        <% }) %>
                    <% } %>
                </div>

                <form class="login-form-real" action="/auth" method="POST">
                    <div class="form-group">
                        <label for="email">E-mail</label>
                        <input type="email" id="email" name="email" required placeholder="seu@email.com">
                    </div>
                    <div class="form-group">
                        <label for="password">Senha</label>
                        <input type="password" id="password" name="password" required placeholder="••••••••">
                    </div>
                    <button class="btn-submit-login" type="submit">Entrar</button>
                </form>

                <div class="login-links">
                    <a href="#">Esqueceu sua senha?</a>
                    <a href="/register">Criar uma conta</a>
                </div>
            </div>
        </section>
    </main>
</body>
</html>